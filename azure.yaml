name: govuk-sre-demo
metadata:
  template: govuk-sre-demo@0.0.1
services:
  frontend:
    project: ./src/frontend
    language: js
    host: containerapp
    docker:
      remoteBuild: true
  backend:
    project: ./src/backend
    language: js
    host: containerapp
    docker:
      remoteBuild: true
infra:
  provider: bicep
  path: infra
